pipeline
	{
	agent any
		stages {
			stage ("Build") {
				steps {
					echo "this is Build"
					sh "git init"
					sh "git pull https://github.com/BharathSukka/sixth.git"
					sh "ls"
					}
				}

			stage ("Deploy") {
				steps {
					echo "this is Deploy"
					}
				}
			}
		}
	
